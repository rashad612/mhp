const test = require('tape'),
      mhp = require('./');

const config = {};

test('custom config', t => {
  t.ok(false, 'config');
  t.end();
}); 

test('server run', t => {
  t.ok(false, 'server run');
  t.end();
});
